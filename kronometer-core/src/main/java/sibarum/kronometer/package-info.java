/**
 * Kronometer's kernel: logical time, shreds, the timeline, the signal graph and precomputation.
 *
 * <p>Empty until M1 — the aggregate build config lands first so the baton spike
 * ({@code kronometer-bench}) can decide the kernel's shape from measurements rather than from
 * assumption. See {@code docs/roadmap.md}.
 *
 * <p>This module takes no runtime dependencies, by rule.
 */
package sibarum.kronometer;
